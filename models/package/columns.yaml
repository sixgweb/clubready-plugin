# ===================================
#  List Column Definitions
# ===================================

columns:
    is_enabled:
        label: Enabled
        type: switch

    name: 
        label: Name
        searchable: true
        displayFrom: package_name

    price: Price

    setup_fee:
        label: Setup Fee

    first_payment_amount:
        label: First Payment Amount

    installments:
        label: Installments
        relation: installments
        relationCount: true