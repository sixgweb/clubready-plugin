# ===================================
#  List Column Definitions
# ===================================

columns:
    is_enabled:
        label: Enabled
        type: switch

    name: 
        label: Name
        searchable: true
        displayFrom: package_name

    price: Price

    installments:
        label: Installments
        relation: installments
        relationCount: true